# v5.0.4 Release Notes

## What Changed

- changed the plugin-managed Codex hook commands to invoke the installed Speak Swiftly hook scripts through `~/.codex/plugins/speak-swiftly/hooks/...`
- updated the Codex hooks doctor so it expects and reports the global tilde-based plugin command path
- aligned the README, hook operating guide, and Speak Swiftly Codex hooks skill with the shipped command path

## Breaking Changes

- None for `SpeakSwiftlyServer` patch consumers.

## Migration And Upgrade Notes

- Upgrade the Speak Swiftly plugin payload so Codex receives the corrected `Stop` and `PermissionRequest` hook commands.
- Restart Codex after upgrading the plugin so the lifecycle hook configuration is reloaded.

## Verification Performed

- Not run at Gale's request.
